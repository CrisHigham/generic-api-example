meta {
  name: Auth: Update User Batch
  type: http
  seq: 6
}

patch {
  url: {{secure}}{{server}}{{base}}/test-user
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
      "email":"spiderman@gmail.com",
      "cellphone":"0123456789"
  }
}

tests {
  let testCode = 403;
  
  test("Auth Update User Batch", function(){
      expect(res.getStatus()).to.equal(testCode)
  })
}
