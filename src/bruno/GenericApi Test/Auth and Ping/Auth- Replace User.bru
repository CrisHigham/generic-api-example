meta {
  name: Auth: Replace User
  type: http
  seq: 7
}

put {
  url: {{secure}}{{server}}{{base}}/test-user/{{userId1}}
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
      "email":"spiderman@gmail.com",
      "cellphone":"0123456789"
  }
}

tests {
  let testCode = 403;
  
  test("Auth Replace User", function(){
      expect(res.getStatus()).to.equal(testCode)
  })
}
